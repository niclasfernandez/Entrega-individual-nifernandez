<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %> 
</head>
<body>

    <header>
        <nav>
            <div class="brand-wrapper">
                <div class="logo">
                    <img src="images/logo.svg" alt="">
                </div>
            </div>        
        </nav>
    </header>

    <main>
        <section class="new">
            <form action="" method="POST">
                <input type="text" name="titulo" id="titulo" placeholder="Título">
                <textarea name="texto" id="texto" cols="30" placeholder="Añade una nota..."></textarea>
                <button type="submit">Crear</button>
            </form>
        </section>
        <section class="list">
                <% for( let i = 0; i < notas.length; i++ ) { %>
                    <div class="note">
                    <h2><%= notas[i].titulo %></h2>
                    <p><%= notas[i].texto %></p>
                </a>
                <div class="edit">
                    <a href="/detail/<%= notas[i].id %>"><i class="far fa-edit"></i></a>
                    <form method="POST" action="?_method=DELETE&id=<%= notas[i].id %>">
                        <button type="submit"><i class="far fa-trash-alt"></i></button>
                    </form>
                </div>
            </div>
            <% } %>   
        </section>
    </main>

</body>
</html>